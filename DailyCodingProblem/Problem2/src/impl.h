#include <vector>

// constraints: nums.size() >= 2
std::vector<int> productArrayBruteForce(std::vector<int>& nums);
std::vector<int> productArrayOpt1(std::vector<int>& nums);
std::vector<int> productArrayOpt2(std::vector<int>& nums);
